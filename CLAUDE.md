# CLAUDE.md

이 파일은 이 저장소에서 작업하는 모든 에이전트의 집필 계약이다.

## 프로젝트 개요

"LLM-Wiki: 지식이 축적되는 AI 작업 환경 만들기" — 한국어 서적 프로젝트.
저자: 안준용 (고려대학교 보건과학대학 바이오시스템의과학부)

내용 설계 정본은 `~/Dropbox/References/llm-wiki/agenda/llm-wiki-book/README.md`다.
집필 설계 정본은 `docs/superpowers/specs/2026-08-04-llm-wiki-book-writing-design.md`다.
장·절 구성을 바꾸려면 내용 설계 정본을 먼저 고친다.

HonKit 기반 마크다운 책. 형식은 `../autism/`, `../human-genetics/`와 동일하다.

## 빌드

```bash
npm install
npm run build      # _book/ 에 HTML 빌드
npm run serve      # localhost:4000 라이브 프리뷰
```

## 구조

```
SUMMARY.md              목차 정본 — 장을 추가하면 반드시 여기에도 추가
part1/chapter1-7.md     파트 1
part2/chapter8-11.md    파트 2
part3/chapter12-16.md   파트 3
appendix/               변동이 빠른 내용 (도구 화면, 명령, 설치 절차)
wiki/                   집필 운영 위키 — 규칙과 상태의 owner
labs/                   재현 검증 실행 기록
images/                 도식과 그림
```

원고는 `partN/`에만 둔다. `wiki/`는 규칙과 상태를 소유하고 원고 문장을 소유하지 않는다.

## 글쓰기 규칙

### 문체

1. 본문에 마크다운 굵은 글씨(`**`)를 쓰지 않는다.
2. 문단은 5문장 이상으로 쓴다. 두세 문장짜리 문단을 이어 붙이지 않는다.
3. AI식 과장을 쓰지 않는다. "완전히 바꾸었다", "혁명적인", "놀랍게도", "극적으로", "단순한 X가 아니라 Y다" 금지.
4. 지시 대용을 쓰지 않는다. "이 사실은", "이것은" 대신 무엇을 가리키는지 명시한다.
5. 내용 없는 섹션 예고를 쓰지 않는다. "이제 살펴보자", "다음 절에서는" 같은 문장으로 분량을 채우지 않는다.
6. 문단 시작을 다양화한다. 모든 문단을 같은 형태로 시작하지 않는다.
7. 목록으로 도망가지 않는다. 설명은 산문으로 쓰고, 목록은 병렬 항목이 실제로 있을 때만 쓴다.
8. 제품명은 개념의 사례로만 쓴다. 기능 목록으로 서술하지 않는다.
9. 독자를 2인칭으로 부르지 않는다. "여러분", "당신" 대신 "독자"를 쓰거나 주어를 생략한다.

### 용어

| 원칙 | 내용 |
|---|---|
| 첫 등장 | 한국어 설명을 붙이고 이후 일관되게 쓴다 |
| 지속적인 작업 기억 | LLM-Wiki를 처음 설명할 때 쓰는 말. knowledge base, context 같은 용어는 그 뒤에 붙인다 |
| 위키 | 완성된 백과사전이 아니라 연결되고 개정되는 문서 체계 |
| 에이전트 | 목표를 위해 파일과 도구를 사용하며 여러 단계를 수행하는 AI. 챗봇과 구분한다 |
| 세션 | 한 번의 작업 흐름. 폴더는 여러 세션에 걸쳐 유지되는 맥락 |

영어 용어는 한국어 정착어가 없을 때만 그대로 쓴다. source note, wiki page, overview, concept, ingest, BM25는 그대로 쓴다. 줄임말은 이후 본문에서 다시 쓸 때만 도입한다.

### 인용과 근거

- 기술적 사실은 `wiki/evidence-ledger.md`에 등록된 것만 쓴다. 등록되지 않은 폴더 이름, 명령, URL, 도구 동작을 쓰지 않는다.
- 확인하지 못한 것은 확인하지 못했다고 쓰거나 쓰지 않는다. 그럴듯한 값을 지어내지 않는다.
- Karpathy의 원래 제안과 이 책의 확장을 항상 구분한다.
- 도구의 현재 동작을 변하지 않는 표준처럼 쓰지 않는다. 화면과 명령은 `appendix/`로 보낸다.

### 개인정보

- 학생, 공동연구자, 이메일 주소, 일정, 미공개 연구 내용, 그랜트 금액과 심사 정보를 원고에 넣지 않는다.
- 파트 3의 사례는 `wiki/corpus.md`의 합성 사례 카드만 쓴다. 카드 밖의 인물을 만들지 않는다.
- 합성 사례는 본문에서 합성이라고 밝힌다.
- 개인별 생산성 비교, 감시, 평가 자동화를 성공 사례로 제시하지 않는다.

## 장 골격

모든 장은 `wiki/chapter-template.md`의 골격을 따른다. 절 제목은 `#`를 쓴다.

## 상태 모델

각 장 파일은 frontmatter에 `status`를 가진다.

`skeleton` → `drafted` → `revised` → `verified` → `final`

`drafted`로 올리려면 사실·문체·개인정보 검증을 통과해야 한다.
`revised`로 올리려면 `<!-- AUTHOR: -->` 마커가 모두 채워져야 한다.
`verified`로 올리려면 실습이 실제로 재현되어야 한다.

현재 상태는 `wiki/index.md`에서 확인한다.

## Git 규칙

- `_book/`, `node_modules/`, `.claude/`는 커밋하지 않는다.
- 장을 추가하거나 상태를 바꾸면 `wiki/log.md`에 한 줄 남긴다.
