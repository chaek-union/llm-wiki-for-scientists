_book/
node_modules/
.DS_Store
.claude/

# 집필 운영 문서. 공개 저장소에 올리지 않는다.
# 근거 원장에 실제 폴더 경로, workspace 운영 방식, 날짜가 붙은 사고 기록이
# 들어 있고, 집필 로그와 설계안에도 내부 판단이 그대로 남아 있다.
# 원고는 이 문서들을 일반화해서 쓰지만 원본은 그렇지 않다.
CLAUDE.md
wiki/
docs/

# 파이썬 캐시
__pycache__/
*.pyc
